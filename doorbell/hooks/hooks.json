{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "_R=\"${CLAUDE_PLUGIN_ROOT}\"; [ -z \"$_R\" ] && { echo '🔔 doorbell: CLAUDE_PLUGIN_ROOT 是空的'; exit 0; }; [ -x \"$_R/scripts/doorbell.sh\" ] || { echo \"🔔 doorbell: 找不到 $_R/scripts/doorbell.sh\"; exit 0; }; \"$_R/scripts/doorbell.sh\"",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
